<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#fff"
    android:clipToPadding="true"
    android:fitsSystemWindows="true"
    android:orientation="vertical">

    <FrameLayout
        android:id="@+id/fl_main_fr"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_weight="1"/>

    <View
        android:layout_width="match_parent"
        android:layout_height="0.5dp"
        android:background="@color/xian" />

    <RadioGroup
        android:id="@+id/rg_bottom_tab"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="#fff"
        android:orientation="horizontal"
        android:paddingTop="5dp">

        <RadioButton
            android:id="@+id/tv_main_zixun"
            style="@style/main_list"
            android:checked="true"
            android:drawableTop="@drawable/seclc_main_zixun"
            android:text="主页" />

        <RadioButton
            android:id="@+id/tv_main_void"
            style="@style/main_list"
            android:drawableTop="@drawable/seclc_main_viod"
            android:text="视频" />

        <RadioButton
            android:id="@+id/tv_main_so"
            style="@style/main_list"
            android:drawableTop="@drawable/secle_main_so"
            android:text="邀请" />

        <RadioButton
            android:id="@+id/tv_main_me"
            style="@style/main_list"
            android:drawableTop="@drawable/seclc_main_me"
            android:text="我的" />

    </RadioGroup>
</LinearLayout>
